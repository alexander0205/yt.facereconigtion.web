import { AllowHyphensDirective } from './allow-hyphens.directive';

describe('AllowHyphensDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowHyphensDirective();
    expect(directive).toBeTruthy();
  });
});
